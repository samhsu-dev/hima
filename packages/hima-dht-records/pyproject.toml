[project]
name = "hima-dht-records"
version = "0.1.0"
description = "Observation record contract: schema constants and payload folding"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
